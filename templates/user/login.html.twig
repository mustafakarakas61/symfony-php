<!DOCTYPE html>
<html lang="tr">
<head>
    <meta charset="UTF-8">
    <title>{% block title %}Giriş Yap{% endblock %}</title>
    <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text></svg>">
    {% block stylesheets %}
        <link rel="stylesheet" href="{{ asset('css/styles.css') }}">
    {% endblock %}
</head>
<body>
{% block body %}
    <div class="container-fluid" style="background-color: #7ebffa; height: 100vh; display: flex; align-items: center; justify-content: center;">
        <div class="card" style="max-width: 400px;">
            <div class="card-header text-center bg-primary text-white">
                <h2 align="center">Giriş Yap</h2>
            </div>
            <div class="card-body">
                <form method="post">
                    <div class="form-group">
                        <label for="username">Kullanıcı Adı:</label>
                        <input type="text" id="username" name="_username" class="form-control" required>
                    </div>
                    <div class="form-group">
                        <label for="password">Şifre:</label>
                        <input type="password" id="password" name="_password" class="form-control" style="margin-left: 55px; margin-top: 5px;" required>
                    </div>
                    <div style="margin-top: 5px;text-align: center">
                        <button type="submit" class="btn btn-primary btn-block">Giriş Yap</button>
                    </div>

                    <div style="text-align: center; margin-top: 10px;">
                        Hesabınız yok mu? <a href="{{ path('app_register') }}">Kayıt olmayı deneyin</a>
                    </div>
                </form>

                {% if error %}
                    <div class="alert alert-danger mt-3" style="margin-top: 4px;text-align: center;color: #9b4141;">
                        {% if error.message == 'Invalid credentials.' %}
                            Geçersiz Kimlik Bilgileri
                        {% elseif error.message =='Bad credentials.' %}
                            Kullanıcı Bulunamadı
                        {% elseif error.message =='The presented password is invalid.' %}
                            Şifreniz Hatalı
                        {% else %}
                            Hata: {{ error.message }}
                        {% endif %}
                    </div>
                {% endif %}
            </div>
        </div>
    </div>
{% endblock %}
</body>
</html>